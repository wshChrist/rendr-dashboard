////////////////////////////////////////////////////////////////////////////////
// Utility function for simulating delays (used in overview charts)
////////////////////////////////////////////////////////////////////////////////

export const delay = (ms: number) =>
  new Promise((resolve) => setTimeout(resolve, ms));
